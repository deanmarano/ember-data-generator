<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">
        <img src="http://emberjs.com/images/ember_logo_x2.png" alt="Ember Data Generator">
        Data Generator</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <form class="navbar-form navbar-right">
        {{#if session.isAuthenticated}}
        <a {{action 'invalidateSession'}} class="btn btn-default">{{fa-icon "github-square"}} Logout</a>
        {{else}}
          {{#link-to 'login' class="btn btn-default"}}
            {{fa-icon "github-square"}} Sign in with Github
          {{/link-to}}
        {{/if}}
      </form>
    </div>
  </div>
</nav>
